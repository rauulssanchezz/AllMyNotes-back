{
  "name": "allmynotes-back",
  "version": "1.0.0",
  "description": "Api for allmynotes app",
  "main": "app.js",
  "scripts": {
    "dev": "node --watch --env-file=.env --experimental-strip-types src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "author": "Raúl Sánchez Sánchez & Jorge Martin Herrera",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "type": "module",
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^5.0.1",
    "@types/jsonwebtoken": "^9.0.9"
  }
}
